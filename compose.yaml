services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 1234567890
      POSTGRES_DB: bookshelf
    ports:
      - 5432:5432
    volumes:
      - PGDATA:/var/lib/postgresql
volumes:
  PGDATA: